# Base URL of the local HAPI FHIR server (server-side only, used by the proxy route)
FHIR_BASE_URL=http://localhost:8080/fhir

# OpenAI API key used by /api/ai/summarize for AI chart summaries
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
